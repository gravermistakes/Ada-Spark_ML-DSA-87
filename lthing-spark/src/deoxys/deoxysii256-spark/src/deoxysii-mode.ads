------------------------------------------------------------------------------
--  Deoxysii.Mode — M-lane internal SCT-2 (SUBAGENT-M owns).
--
--  SUBAGENT-M fills at M1 with the SCT-2 machinery (DEOXY_1_41.md M-2..M-5)
--  over the frozen Deoxysii.TBC seam: domain-separation tweaks (M-2 table),
--  pad10 (M-1), E^II (M-3), forward-only D^II (M-4), constant-time verify
--  via Deoxysii.CT (M-5). Never opens the TBC body.
--  Structure scaffolded at G0; contents are M's.
------------------------------------------------------------------------------

pragma SPARK_Mode (On);

private package Deoxysii.Mode is

end Deoxysii.Mode;
